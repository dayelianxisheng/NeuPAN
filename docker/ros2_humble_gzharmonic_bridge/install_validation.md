# Installation validation

The authoritative candidate, dependency simulation, installed package list,
bridge type registry, and immutable output image ID are recorded under the
Stage 11C-A artifact directory. No packages are installed into `ros2_dev`.
